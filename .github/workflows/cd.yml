name: Release automation disabled

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  manual-first-alpha:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "The first alpha is manual from an exact reviewed main commit."
          echo "Use the release packaging workflow in dry-run mode until publication is explicitly approved."
          exit 1
