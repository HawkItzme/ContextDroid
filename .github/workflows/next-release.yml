name: Inherited next-release automation disabled

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "ContextDroid does not use inherited next-release automation for v0.1.0-alpha.1."
          exit 1
