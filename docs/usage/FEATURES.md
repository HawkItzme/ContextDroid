# ContextDroid features

This inherited path is retained for stable links. The current product surface is documented in
the repository [README](../../README.md), [filter matrix](../FILTER_MATRIX.md), and
[safety contract](../SAFETY_CONTRACT.md).

RTK-compatible commands are available explicitly and through the opt-in `rtk-compatible`
profile. They are not evidence that ContextDroid safe profiles automatically rewrite those
command families.
