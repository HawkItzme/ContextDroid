# Local audit guide

Use `contextdroid gain --history`, `contextdroid session`, `contextdroid discover`, and
`contextdroid quality` for local audit data. ContextDroid does not send remote telemetry and does
not reuse inherited RTK savings percentages. See [Benchmarks](../BENCHMARKS.md) for methodology.
