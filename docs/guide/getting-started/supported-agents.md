# Supported agent integrations

Claude Code on Linux is supported. Cursor hooks schema version 1 is experimental and opt-in.
Codex is guidance-only: it
receives a bounded `AGENTS.md` block because transparent shell interception is not claimed. Use
`contextdroid setup --help` and see [Integrations](../../INTEGRATIONS.md).
