# Supported agent integrations

Claude Code on Linux is the supported alpha candidate. Cursor hooks schema version 1 is
experimental until cross-platform release-commit smoke is recorded. Codex is guidance-only: it
receives a bounded `AGENTS.md` block because transparent shell interception is not claimed. Use
`contextdroid integrations --help` and see [Integrations](../../INTEGRATIONS.md).
