# Gain and quality reports

Use `contextdroid gain` for local estimated reduction and `contextdroid quality` for confidence,
fallback, parser, fixture, and recovery proxies. `--scope`, `--command`, `--project`, and `--since`
filters are available. See [Benchmarks](../../BENCHMARKS.md); estimates are not model-session billing.
