# Discovery and quality

`contextdroid discover` and `contextdroid session` retain inherited local session analysis.
Run-centric correctness and savings proxies are reported by `contextdroid gain` and
`contextdroid quality`. These commands use local data only and do not claim complete model-session
billing savings.
