# Inherited RTK compatibility

ContextDroid retains useful explicit RTK-derived command families. Automatic inherited coverage
is available only in the opt-in `rtk-compatible` profile and still obeys universal hard stops.
The authoritative automatic-rewrite matrix is [FILTER_MATRIX.md](../../FILTER_MATRIX.md).
